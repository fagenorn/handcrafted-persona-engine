# assets-source/

Local-only staging for assets uploaded to https://huggingface.co/fagenorn/persona-engine-assets.

Layout mirrors HuggingFace repo paths exactly. After dropping files here,
run `scripts/hash-manifest.ps1` and `scripts/upload-hf-assets.ps1`.
